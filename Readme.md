
View this readme ctrl + Shift + V

# run tailwind in dev mode
 npx tailwindcss -i ./src/input.css -o ./src/output.css --watch
  
 takes a while to boot up.. 
Then run the golive server

appshell.html list the tailwind side nav with main nav
